#include "battery.h"
void fetch_battary(int times)
{
  // battery.begin();
  // int battery_percentage = 0;
  // float battery_voltage = 0;
  // for (int i = 0; i < times; i++)
  // {
  //   battery.getMeasurement();
  //   battery_percentage = battery_percentage + battery.batteryPercentage;
  //   battery_voltage = battery_voltage + battery.batteryVoltage;
  // }
  // battery_percentage = battery_percentage / times;
  // global_sensor_data.battery_voltage = battery_voltage / times;
  // global_sensor_data.battery_level = battery_percentage * 255 / 100;
}
